trigger: none
pr: none

# Pipeline resource defined as per https://bit.ly/3PhwhRk
resources:
  pipelines:
  - pipeline: build_pipeline        # Used to reference the pipeline reference from other parts of the code.
    source: testallpipelines_build_pipeline  	# This must match  the build pipeline name in ADO.
                                    # By default, pipelines are named after the ADO build pipeline name.
    trigger:
      branches:
      - master                      # Will get triggered whenever the source pipeline runs on master branch.

jobs:
- deployment: testallpipelines_deploy_to_appservice
  displayName: TestAllPipelines Deploy to AppService
  environment: production
  pool:
    vmImage: 'ubuntu-latest'
  strategy:
    runOnce:
      deploy:
        steps:
        - task: PowerShell@2
          displayName: 'Create version number'
          name: 'CreateVersionNumber'
          inputs:
            targetType: 'inline'
            script: |
              $newVersion = "$(resources.pipeline.build_pipeline.runName)"
              [string] $buildName = "$($newVersion)"
              Write-Host "##vso[build.updatebuildnumber]$buildName"

        - download: none

        - task: DownloadPipelineArtifact@2
          displayName: 'Download Artifact TestAllPipelines.Api'
          inputs:
            buildType: 'specific'
            definition: testallpipelines_build_pipeline
            buildVersionToDownload: 'latest'
            artifactName: 'drop.TestAllPipelines.Api'
            project: 'DeleteMeAfterYouSetupThosePipelines'
            downloadPath: '$(Pipeline.Workspace)/drop/TestAllPipelines.Api'

        - task: DownloadPipelineArtifact@2
          displayName: 'Download Artifact TestAllPipelines.WorkerServices'
          inputs:
            buildType: 'specific'
            definition: testallpipelines_build_pipeline
            buildVersionToDownload: 'latest'
            artifactName: 'drop.TestAllPipelines.WorkerServices'
            project: 'DeleteMeAfterYouSetupThosePipelines'
            downloadPath: '$(Pipeline.Workspace)/drop/TestAllPipelines.WorkerServices'
